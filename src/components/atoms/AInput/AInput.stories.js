import AInput from './AInput.vue'

const meta = {
    title: 'Components/AInput',
    component: AInput,
    tags: ['autodocs'],
    argTypes: {},
}

export default meta

export const Default = {
    args: {},
}
